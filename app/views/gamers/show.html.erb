<%= @gamer.email %>

<!-- Check if current_user == @user -->
<%=link_to 'Update your profile', edit_gamer_path(@gamer)%>

<% if user_signed_in? && @gamer.id == current_user.id %>
  It's you
<% end %>
