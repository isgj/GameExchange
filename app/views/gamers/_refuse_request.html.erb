<%= form_for(@gamer.passive_requests.find_by(applier_id: applier),
  html: { method: :delete }) do |f| %>
  <div><%= hidden_field_tag :applier_id, applier %></div>
  <%= f.submit "Refuse", class: "btn btn-primary" %>
<% end %>
